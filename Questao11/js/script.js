function multiplicacao (num1,num2) {
    let resultado = num1 * num2;
    return resultado;
   }
   multiplica(4, 7);
   multiplica(5, 6);
   multiplica(3, 3);
   // Não haverá nenhum retorno pois a função não está sendo chamada

   // Segunda resposta é letra(c) a propriedade !important